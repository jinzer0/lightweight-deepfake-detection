inherits: default.yaml
dataset:
  name: cifake
mode: fusion
features:
  clip:
    enabled: true
  frequency:
    enabled: true
classifier:
  name: logistic_regression
  params:
    max_iter: 1000
    random_state: 42
